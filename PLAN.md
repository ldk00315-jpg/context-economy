# 実装計画と終了条件

2026-09-08。接続先を限定しない検証用Pythonライブラリ。

目的は「作業成功と回答の正確さを維持しながら、完了までの入力・出力トークンを減らす」。任意の問題に対する最大削減率や同一回答を保証するものではない。初版では自動推論量削減やモデルの小型化を行わない。

## 構成

1. **lossless-first**：JSONの空白除去・列名共有、連続する同一行の回数表現を候補にし、説明ヘッダーまで含む実トークン数が最小の表現を選ぶ。必ず独立復号チェックを通す。コードはbyte-exactで保持。数字配列や全件列挙を勝手に間引かない。
2. **正確な問い合わせ**：原文全件に対する型付き検索・JSON Pointer・Decimal集計をPythonで処理。呼び出し側が指定した操作の範囲・母数・一致数を明示。推測したユーザー意図で集計へ置き換えない。
3. **明示的な部分取得**：原文をSQLiteへ確定保存した後に限り、focus/readで抜粋と範囲・未取得件数・参照IDを返す。完全網羅と誤認させない。期限による自動削除をしない。保存や整形の失敗時は元の入力へ戻す。
4. **品質と総量の測定**：語彙の一部が残るだけでは合格としない。全件復号、厳密集計、指定項目の検索、追加取得の完遂を検証する。payload計数とモデル課金usageを分ける。追加呼出しやツール定義を含む評価口を提供する。

## 初版の終了条件

- 無断間引き・数値丸め・コード書換えがないことをテスト。
- 保存→終了→再オープン→正確な復元、破損と保存失敗時の挙動をテスト。
- JSON・ログ・数値・ファイルパス・日本語・コード・短文を含む固定seedの比較を記録。
- 低予算では「予算達成のための不可視欠落」をせず、超過を明示。
- 原文が必要なケースでは追加取得コストによって節約が悪化する結果も掲載。
- 外部モデルに依存しないCLIとPython API、strict grader付きのプロバイダー非依存評価コールバックを提供。

## 次段階の検証条件

2026-09-08合意更新：モデル出力長の変動は許容し、接続先ごとのアダプターは標準機能の要件から外す。prepare_input / CLI prepareを通常入口として実装。最終可視入力の計数に基づく非増加、完全性または明示操作範囲の保持、モデル呼出しゼロが範囲。厳密な総量上限のguardは任意機能として残す。66テストと既存6fixtureのローカル再生を確認。results/preparation/RESULTS.md。

2026-09-08安全方針の更新：平均削減より個別タスクの増加防止を優先。Routerの確率ベース探索許可を撤去し、すべて完全情報へ戻す。plan_callで最終入力＋出力上限を確認し、SingleCallGateで永続的な一度限りの送信を提供。入力上限不明・予算不足は送信前拒否。SDKの物理呼出し数と出力上限を強制できる接続先のみ接続可。Astra CLIへの接続は未実装・未検証。結果は results/no-spike/RESULTS.md。

2026-09-08追記：Routerを実装。呼出し側が明示したcomplete/aggregate/select/lookup/exploreに対し、完全圧縮を基準に選択する。自由文の意図推定は範囲外。公開設定・コード2件と合成業務データ4件を3条件で測定し、全問正解、総トークン36.7%減（従来圧縮から16.6%減）。実施範囲と限界は results/routing/RESULTS.md。汎用非劣性・自律参照解決は未実証。

実LLMの品質優越は初版の機械テストだけでは判定しない。同じモデル・設定・課題のpaired試験を行い、各seed/反復、全API usage、失敗・再試行・復元を含める。全件列挙は完全一致、集計はDecimal一致、コードはテスト成功、一般QAは独立した厳密基準を使う。成功率差の許容値（初期候補1ポイント）は、用途とサンプル数を決めてから固定する。100例で差が見えないだけでは非劣性と呼ばない。

## 参照した公式資料

- https://www.anthropic.com/engineering/code-execution-with-mcp — 中間データをモデルに渡さず、ローカルで必要な操作を行う設計。
- https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents — 必要な時点で参照から読む設計。
- https://developers.openai.com/api/docs/guides/prompt-caching — 履歴の既存prefixを保持すること、トークン削減とcache割引の区別。初版はAPI設定を変更しない。

本実装の削減率・品質はこれらの他社事例から借用せず、独自測定で示す。
