# 次の検証：実LLMでの小規模paired試験

追試：欠落証拠の回復をAstraサブスクで4問・3呼び出し実測。必要な3資料だけを取得し全問正解したが、総トークンは84.5%増。詳細は results/retrieval/RESULTS.md。今回の追加検証は完了。

状態：2026-09-08、Astra（gpt-6-astra / medium）を既存ChatGPTサブスク認証のCodex CLIで実測済み。利用枠2%以内という希望を優先し、下記40試行は実施せず、6問をまとめた原文／最適化の2呼び出しで終了。結果は results/subscription/RESULTS.md。下記は将来の拡張案であり、実施済みの範囲ではない。

## 目的

圧縮データの機械的復元ではなく、モデルが正しく読み、必要時に原文へ戻り、同じ課題を少ない総トークンで完了できるかを確認する。

## 初回の固定範囲

10課題 × 2反復 × 原文/最適化の2条件 = 40試行。ツール継続によるモデル呼び出しは試行数とは別に数える。同一モデル・推論設定・出力上限・ツール定義で比較し、条件の実行順序を入れ替える。

| 課題 | 件数 | 判定 |
|---|---:|---|
| 表形式の特定行・例外行 | 2 | レコード全体が一致 |
| 小数・大きな整数を含む集計 | 2 | 丸めなしの厳密一致 |
| 全ファイルパスの列挙 | 1 | 欠落・重複・順序違いなし |
| 反復ログの回数・順序 | 1 | 件数と順序の一致 |
| 日本語の否定・例外条件 | 1 | 固定した正解JSONとの一致 |
| 離れた箇所の情報を組み合わせる | 1 | 必要な根拠を使った正解 |
| 抜粋だけでは答えられず原文取得が必要 | 1 | 取得して正解。取得漏れは失敗 |
| 節約できないコード等の対照例 | 1 | 原文維持と正解 |

既存の4課題はアダプター確認用なので、上記の本試行前に拡張する。正解・評価ルールはモデル出力を見る前に固定する。探索の条件選択を含む実験と、明示的な集計結果を渡す実験は分けて報告する。

## 計数・予算

- 全API呼び出しの実usageを保存。入力・出力・推論・ツール継続・再試行を含める。
- ツール定義、原文参照、追加取得で再送される履歴も含む。
- 総トークンとキャッシュ割引を含む実費を別々に比較する。
- 1試行のモデル呼び出し上限と総予算を実行前に設定。予算の残額に対して次の呼び出しの最大費用を確保できなければ停止する。
- 認証・課金情報はレポートに出さない。usage不明の失敗を0円・0トークンとして節約に加えない。

## 終了条件

初回結果は、全課題の正誤・品質回帰・総量・追加取得率を一括報告する。明確な欠陥が出た場合は修正をまとめ、同じ課題と変更の直接影響を1回確認する。小規模試験の成功だけでは品質非劣性を主張しない。

次の大規模評価はこの結果で判断する。少数例の有利な削減率を、そのまま一般的な品質保証や実運用の費用削減率にしない。
